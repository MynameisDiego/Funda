<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="pe.edu.upc.GlucoCheck">
    <uses-permission android:name="android.permission.CALL_PHONE"/>
    <application

            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".presentation.sign_up.SignUpActivity">
        </activity>
        <activity android:name=".presentation.home_menu.HomeMenuActivity">
        </activity>
        <activity android:name=".presentation.cita.CitaActivity">
        </activity>
        <activity android:name=".presentation.educando.EducandoMainActivity"
            android:label="@string/title_activity_educando"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>

        <activity android:name=".presentation.nuevo_paciente.NuevoPacienteActivity">
    </activity>

        <activity android:name=".presentation.paciente.PacienteActivity">
        </activity>

        <activity android:name=".presentation.glucosa.GlucosaActivity">
        </activity>

        <activity android:name=".presentation.reporte.ReporteActivity">
        </activity>


        <activity android:name=".presentation.information.MyInformationActivity">
        </activity>

        <activity android:name=".presentation.home.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

    </application>

</manifest>